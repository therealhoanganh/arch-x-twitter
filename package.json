{
  "name": "arch-x-twitter",
  "version": "0.10.0",
  "description": "Obsidian plugin that bulk-archives X/Twitter profiles and posts into the vault.",
  "private": true,
  "scripts": {
    "build": "node build.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
